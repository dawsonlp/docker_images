# PostgreSQL Configuration
# IMPORTANT: Copy this file to .env and set strong passwords
# Never commit .env to git - it's in .gitignore

# PostgreSQL Credentials
# SECURITY: Use strong passwords with at least 16 characters including:
# - Uppercase and lowercase letters
# - Numbers
# - Special characters (!@#$%^&*()_+-=[]{}|;:,.<>?)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
POSTGRES_DB=postgres

# Optional: Load TIGER data for specific states (comma-separated)
# WARNING: This significantly increases startup time and storage
# Example: TIGER_STATES=CA,NY,TX
# Recommended: Leave empty and load data manually after startup
TIGER_STATES=

# Optional: Set timezone
TZ=UTC

# pgAdmin Configuration (optional service)
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD

# Security Best Practices:
# 1. Generate strong random passwords: openssl rand -base64 32
# 2. Use different passwords for each service
# 3. Never commit .env to version control
# 4. In production, use secrets management (AWS Secrets Manager, Kubernetes Secrets, etc.)
# 5. Rotate passwords regularly
# 6. Use SSL/TLS for connections in production
